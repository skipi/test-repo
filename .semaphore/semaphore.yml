version: v1.0
name: Test Results
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
promotions:
  - name: compile
    pipeline_file: compile.yml
    auto_promote:
      when: "result = 'passed' and branch != 'master'"
blocks:
  - name: Compile
    skip:
      when: "branch = 'master'"
    task:
      jobs:
        - name: 'Something'
          commands:
            - echo 0